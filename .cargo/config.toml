# 默认把 SaySo 编成 Windows MSVC ABI 的 .exe。
# 配合 `cargo-xwin`（用户级，无需 root，自动拉 Windows SDK 片段）使用：
#   cargo xwin build --release
# 仍可手动 `cargo build --target x86_64-unknown-linux-gnu` 跑单元测试。
[build]
target = "x86_64-pc-windows-msvc"
